options:
    cost-multiplier: 0.5
    min-cost: 1

on enchant:
    set {_original} to experience cost
    set {_reduced} to floor({_original} * {@cost-multiplier})
    set {_final} to max({@min-cost}, {_reduced})
    set experience cost to {_final}
